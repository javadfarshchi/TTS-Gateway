# Kokoro-82M Service Configuration
# Copy this file to .env and fill in your values

# Optional API key for securing the service
# If not set, the service will be open (no authentication)
KOKORO_API_KEY=your_secret_api_key_here

# Model configuration (optional overrides)
# MODEL_NAME=hexgrad/Kokoro-82M
# MAX_PROMPT_LENGTH=1000
# MAX_OUTPUT_TOKENS=200

# --- Kokoro provider settings ---
# Paths can be relative to the project root
# KOKORO_MODEL_PATH=models/kokoro/kokoro-v1.0.onnx
# KOKORO_VOICES_PATH=models/kokoro/voices-v1.0.bin
# KOKORO_DEFAULT_VOICE=af_alloy
# KOKORO_DEFAULT_LANG=en-us
# KOKORO_ESPEAK_LIBRARY=/usr/lib/libespeak-ng.so
# KOKORO_ESPEAK_DATA=/usr/share/espeak-ng-data

# Hugging Face token (if needed for private models)
# HF_TOKEN=your_huggingface_token_here
